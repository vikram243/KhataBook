<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KhataBook</title>
    <link rel="icon" type="image/x-icon" href="/public/images/logo.png">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="navbar flex justify-between w-full px-20 py-10">
        <h3 class="text-2xl font-bold"> Khaatabook <i class="ri-git-repository-line"></i></h3>
        <% var error=typeof error !=='undefined' ? error : "" %>
        <% if(error.length> 0){ %>
                <h3 class="absolute top-10 left-1/2 -translate-x-1/2 -translate-y-1/2 px-3 py-2 rounded-md text-red-600">
                    <%= error %>
                </h3>
        <% } %>
        <% var loggedin=typeof loggedin !=='undefined' ? loggedin : true %>
            <% if(loggedin){ %>
                <div class="links flex gap-10">
                    <a href="/profile"><i class="ri-home-5-line"></i> Home </a>
                    <a href="/hisaab/create"><i class="ri-file-add-line"></i> Create new hisaab </a>
                    <a class="text-red-500" href="/logout"><i class="ri-logout-circle-r-line"></i> Logout </a>
                </div>
                <% } %>
    </div>